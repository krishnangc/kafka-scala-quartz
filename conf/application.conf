# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
play.crypto.secret="Maybe you want to change this..."

# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Database configuration
# ~~~~~

slick.dbs.default.driver="utils.db.TetraoPostgresDriver$"
slick.dbs.default.db.driver="org.postgresql.Driver"
slick.dbs.default.db.url="jdbc:postgresql://128.199.132.83:5432/qubida-test"
slick.dbs.default.db.user="postgres"
slick.dbs.default.db.password="datalynx11"

play.filters.csrf.cookie.name="PLAY_CSRF_TOKEN" // We store the CSRF token in a cookie instead of the session so that Angular can read it
play.filters.csrf.contentType.blackList=[ // We would also check AJAX requests from type application/json
  "application/x-www-form-urlencoded",
  "multipart/form-data",
  "text/plain",
  "application/json"
]

scheduler {
    kafka {
        producer = "localhost:6667" 
		consumer = "localhost:2181"
    }
}